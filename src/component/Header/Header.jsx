import PropTypes from 'prop-types';

const Header = () => {
    return (
        <div>
            <h1>Course Registration</h1>
        </div>
    );
};

Header.propTypes = {
    
};

export default Header;